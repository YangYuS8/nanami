# nanami-ui

Qt/QML skeleton for Nanami desktop UI.

This app is presentation-only at this stage. It does not execute commands, call OpenClaw, or call CubeSandbox directly.

## Build

```bash
cmake -S . -B build -G Ninja
cmake --build build
```
